---

- name: Install fzy
  become: true
  package:
    name: fzy
    state: present
  when: ansible_facts["os_family"] != "Darwin"

- name: Install fzy
  homebrew:
    name: fzy
    state: present
  when: ansible_facts["os_family"] == "Darwin"
