---

- name: Install Python and some libs
  become: true
  dnf:
    name:
      - python3
      - python3-devel
      - python3-pygments
      - python3-pip
  when: ansible_facts["os_family"] == "RedHat"
