---
- hosts: localhost
  tasks:
    - name: "Install docker"
      dnf:
        name: "{{item}}"
        state: present
      become: true
      with_items:
        - docker
        - docker-compose

    - include: shared_tasks/docker.yml
